Fix New-chat doing nothing + clickable conversation cards + a Skills browser

- New chat was a no-op: the load-race guard returned early when both conversationId
  and loadingId were null (null===null), so the old thread never cleared. Guard
  loadingId only for real ids.
- Conversation list is now full-width clickable cards (whole card selects), with
  copy/rename/delete icons floating over the right edge — always visible on mobile,
  hover-reveal on desktop. Fixes the finicky click target.
- New Skills browser: /api/skills + SkillsModal listing every installed skill with
  its description, expandable to the full playbook (Markdown). Opened from a new
  "Skills" entry in the sidebar.
